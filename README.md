# rpp

A pure C Linux tool built with Autotools.

## Installation

### From Source
```bash
autoreconf -i
./configure
make
sudo make install
```

### From Debian Package
```bash
sudo dpkg -i rpp_*.deb
```

## Usage
Run `rpp --help` for a list of available commands.